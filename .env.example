# PostgreSQL connection used by Prisma.
# URL-encode special characters in the username or password.
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/machine_translation?schema=public"

# Replace with a long, random value. Do not reuse this example in production.
JWT_SECRET="replace-this-with-a-long-random-secret"
